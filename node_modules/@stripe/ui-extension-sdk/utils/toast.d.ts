export declare type ToastType = 'success' | 'caution' | 'pending' | undefined;
interface BaseOptions {
    type?: ToastType;
}
interface WithAction {
    action: string;
    onAction: () => void;
}
export declare type ToastOptions = BaseOptions | (BaseOptions & WithAction);
export declare type ShowToast = (message: string, options?: ToastOptions) => Promise<{
    update: (updateMessage: string, updateOptions?: ToastOptions) => void;
    dismiss: () => void;
}>;
export declare const showToast: ShowToast;
export {};
